"use client";
import {FormEvent,useState} from "react";
import {createSupabaseBrowserClient} from "../../lib/supabase-browser";
export default function Login(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [signup,setSignup]=useState(false);const [msg,setMsg]=useState('');async function submit(e:FormEvent){e.preventDefault();setMsg('');try{const supabase=createSupabaseBrowserClient();const result=signup?await supabase.auth.signUp({email,password}):await supabase.auth.signInWithPassword({email,password});if(result.error)throw result.error;if(signup){setMsg('Account created. Check your email if confirmation is enabled.')}else{window.location.href='/dashboard'}}catch(err){setMsg(err instanceof Error?err.message:'Authentication failed.')}}return <main className="auth"><section className="auth-card"><div className="brand"><span>G</span> GOD OF INNOVATION</div><small>SECURE ACCESS</small><h1>{signup?'Create your workspace':'Welcome back'}</h1><p>{signup?'Start building your innovation workspace.':'Enter the command center.'}</p><form onSubmit={submit}><input type="email" required placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input type="password" required minLength={6} placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)}/><button>{signup?'Create account':'Sign in'}</button></form>{msg&&<div className="auth-msg">{msg}</div>}<button className="switch" onClick={()=>{setSignup(!signup);setMsg('')}}>{signup?'Already have an account? Sign in':'New here? Create an account'}</button></section></main>}
