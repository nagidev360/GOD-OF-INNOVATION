"use client";
import {useState} from 'react';
import {Sparkles,Send,Loader2} from 'lucide-react';
export default function AIPage(){const [prompt,setPrompt]=useState('');const [output,setOutput]=useState('');const [loading,setLoading]=useState(false);const [error,setError]=useState('');async function run(){if(!prompt.trim())return;setLoading(true);setError('');setOutput('');try{const r=await fetch('/api/ai',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({prompt})});const d=await r.json();if(!r.ok)throw new Error(d.error||'AI request failed');setOutput(d.output||'No response returned.')}catch(e){setError(e instanceof Error?e.message:'AI request failed.')}finally{setLoading(false)}}return <main className="ai-page"><div className="ai-head"><div><div className="badge"><Sparkles size={13}/> AI INNOVATION ENGINE</div><h1>Build the impossible.</h1><p>Describe an idea, problem or project. The engine turns it into a practical execution path.</p></div><a href="/dashboard">Dashboard</a></div><section className="ai-box"><textarea value={prompt} onChange={e=>setPrompt(e.target.value)} placeholder="What do you want to build?" maxLength={12000}/><div><span>{prompt.length}/12000</span><button onClick={run} disabled={loading}>{loading?<Loader2 className="spin" size={18}/>:<Send size={18}/>} {loading?'Thinking':'Run AI'}</button></div></section>{error&&<div className="ai-error">{error}</div>}{output&&<article className="ai-output"><small>AI OUTPUT</small><pre>{output}</pre></article>}</main>}
