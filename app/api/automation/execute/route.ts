import {NextResponse} from "next/server";
import {createSupabaseServerClient} from "../../../../lib/supabase-server";
import {executeAutomationWorkflow} from "../../../../lib/automation-executor";
export async function POST(request:Request){try{const supabase=await createSupabaseServerClient();const {data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({error:"Authentication required."},{status:401});const body=await request.json();const id=typeof body.id==="string"?body.id:"";if(!id)return NextResponse.json({error:"Workflow id is required."},{status:400});const {data:w,error}=await supabase.from("automation_workflows").select("id,name,description,trigger_type,enabled,config").eq("id",id).eq("user_id",user.id).single();if(error)return NextResponse.json({error:"Workflow not found."},{status:404});if(!w.enabled)return NextResponse.json({error:"Workflow is disabled."},{status:400});const run=await executeAutomationWorkflow(supabase,w,user.id,"manual",{});return NextResponse.json({run});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Execution failed."},{status:500});}}