const base=process.env.APP_URL; const secret=process.env.AUTOMATION_CRON_SECRET; if(!base||!secret)throw new Error("APP_URL and AUTOMATION_CRON_SECRET are required."); const response=await fetch(new URL("/api/automation/scheduler",base),{method:"POST",headers:{Authorization:`Bearer ${secret}`}}); const text=await response.text(); console.log(text); if(!response.ok)process.exit(1);